bin/
obj/
*.user
*.suo
.vs/
node_modules/
*.exe
# Installer binaries are distributed via GitHub Releases, not the repo —
# they exceed GitHub's 100MB push limit.
# Portable zip bundles too — also released, not committed.
*.zip
# Scratch stdout/stderr dumps
Symbolic.Wpf/e.txt
Symbolic.Wpf/o.txt
# Scratch repl files at repo root
/minimal.cpd
/minimal.html
/integral_demo.cpd
/integral_demo.html
# Temporary debug project used while diagnosing AngouriMath stack overflow.
TestAngouri/
